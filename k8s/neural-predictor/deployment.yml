apiVersion: apps/v1
kind: Deployment
metadata:
  name: neural-predictor
  namespace: team-neural
spec:
  replicas: 1
  selector:
    matchLabels:
      app: neural-predictor
  template:
    metadata:
      labels:
        app: neural-predictor
        team: neural
    spec:
      containers:
        - name: neural-predictor
          image: ghcr.io/czarnick89/neural-predictor:latest
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: neural-predictor-config
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
      imagePullSecrets:
        - name: ghcr-secret
