apiVersion: v1
kind: ConfigMap
metadata:
  name: neural-predictor-config
  namespace: team-neural
data:
  ENDPOINT_NAME: "neural-predictor-endpoint"
  AWS_REGION: "us-east-1"
  LOG_LEVEL: "info"
  MOCK_MODE: "true"
  MODEL_VERSION: "v1.0"
