apiVersion: v1
kind: Service
metadata:
  name: recommendations-service
  namespace: team-recommendations 
spec:
  type: LoadBalancer
  selector:
    app: recommendations-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000