apiVersion: v1
kind: Service
metadata:
  name: example-service
  namespace: team-example
spec:
  type: LoadBalancer
  selector:
    app: example-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
